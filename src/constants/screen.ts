export enum SCREEN {
  XS = 'xs',
  SM = 'sm',
  MD = 'md',
  LG = 'lg',
  XL = 'xl',
}

export enum BREAKPOINT {
  XS_MIN = 0,
  SM_MIN = 570,
  MD_MIN = 768,
  LG_MIN = 1024,
  XL_MIN = 1300,
}

export enum DEVICE_PLATFORM {
  DESKTOP = 'desktop',
  MOBILE = 'mobile',
  TABLET = 'tablet',
}
