export enum LOCALE {
  EN = 'en',
  ZH = 'zh',
}

export const locales = [LOCALE.EN, LOCALE.ZH];

export const DEFAULT_LOCALE = LOCALE.EN;

export const timeZone = 'Asia/Hong_Kong';

export const localePrefix = 'never';

export const NEXT_LOCALE_COOKIE_KEY = 'NEXT_LOCALE';
